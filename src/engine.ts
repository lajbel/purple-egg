import kaboom from "kaboom";

export const k = kaboom({
    width: 160,
    height: 120,
    pixelDensity: 1,
    crisp: true,
    font: "fb",
    stretch: true,
    letterbox: true,
    plugins: [],
});
